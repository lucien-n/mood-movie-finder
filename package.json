{
  "name": "mood-movie-finder",
  "private": true,
  "workspaces": [
    "back",
    "front",
    "common"
  ],
  "scripts": {
    "build:common": "pnpm build -w common",
    "build:back": "pnpm build:common && pnpm build -w back",
    "build:front": "pnpm build:common && pnpm build -w front"
  }
}
