{
  "name": "mood-movie-finder",
  "private": true,
  "workspaces": [
    "back",
    "front",
    "common"
  ],
  "scripts": {
    "build:common": "npm run build -w common",
    "build:back": "npm run build:common && npm run build -w back",
    "build:front": "npm run build:common && npm run build -w front"
  }
}
